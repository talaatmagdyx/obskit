"""
obskit integrations — DB (SQLAlchemy, psycopg2, psycopg3), Queues (Kafka, RabbitMQ), gRPC.

Install extras:
    pip install obskit[sqlalchemy]    # SQLAlchemy
    pip install obskit[psycopg2]      # psycopg2 (sync)
    pip install obskit[psycopg3]      # psycopg3 (sync + async)
    pip install obskit[db]            # all DB drivers
    pip install obskit[kafka]         # Kafka
    pip install obskit[rabbitmq]      # RabbitMQ
    pip install obskit[grpc]          # gRPC
    pip install obskit[integrations]  # everything
"""
