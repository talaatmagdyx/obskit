"""Unit tests for obskit.

Unit tests are fast, isolated tests that test individual components
without external dependencies (no network, no database, no Redis).

Guidelines:
- Each test file should focus on a single module
- Use mocks for external dependencies
- Tests should run in < 1 second each
- No side effects between tests
"""
